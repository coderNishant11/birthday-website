# Birthday Website — Milestone 1

Flow:
3-2-1 countdown → reminder → Open Surprise → unlit cake → Decorate → cake decorations + candles light → Blow the Candles → flames go out → upward confetti → Happy Birthday → stars + balloons + hearts.

Customize `js/config.js`. Open `index.html` in a modern browser. No JS libraries are required.
